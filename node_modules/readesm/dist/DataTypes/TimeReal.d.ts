import Block from 'DataTypes/Block';
export default class TimeReal extends Block {
    time: Date;
    constructor(data: ArrayBuffer);
    date(): Date;
    static fromTime_t(seconds: number): Date;
    toString(): string;
}
