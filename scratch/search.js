const fs = require('fs'); const content = fs.readFileSync('index.html', 'utf8'); ['lastDownload', 'scaricoTachigrafo'].forEach(word => { console.log('\n--- Matches for ' + word + ' ---'); const regex = new RegExp('.{0,40}' + word + '.{0,40}', 'g'); let match; let count=0; while ((match = regex.exec(content)) !== null && count < 10) { console.log(match[0].replace(/\n/g, ' ')); count++; } });
